{
  "name": "next-and-react-native-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cd viteApp && npm run dev",
    "expo": "cd expoapp && npx expo start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "workspaces": [
    "viteApp",
    "expoapp"
  ],
  "oldInstallScript": "cd viteApp && npm install && cd ../expoapp && npm install"
}
